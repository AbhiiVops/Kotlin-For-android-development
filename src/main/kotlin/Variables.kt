fun main() {

    var wholeNumber = 10
    var fractionalNumber = 3.5
    var sentence = "A new sentence"
    var condition = false
    var character = 'a'

    var a: Int = 5
    var b: String = "Internshala"

    val number = 2345
    val decimal = 5.6
    val name = "Internshala"
    val conditional = true
    val floatnumber = 36.5f

    wholeNumber += 40

    // decimal += 30.2 --> Gives an error saying that val cannot be reassigned

    // wholeNumber += 10.5 --> Type mismatch (Statically typed language)

    println(wholeNumber)
    println(fractionalNumber)
    println(sentence)
    println(condition)

    println(number)
    println(decimal)
    println(name)
    println(conditional)


}